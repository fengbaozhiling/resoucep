In('placeholder','rele_fast');
/*select*/
In('chosen',function(){
   	$(".selectstyle").chosen();
});

//滚动的初始化
startmarquee(24,100,2000,1);/**startmarquee(一次滚动高度,速度,停留时间,图层标记);**/
if ($('#flashLine').length>0){
	In('bannerset');
}
if ($('#jcarousel_scroll').length>0){
	In('jcarousel',function(){
		$('#jcarousel_scroll').jcarousel({
			auto: 2,
			wrap: 'last',
			buttonNextHTML: '<a href="javascript:;" class="next"></a>',
			buttonPrevHTML: '<a href="javascript:;" class="prev"></a>'
		});
	})
}
